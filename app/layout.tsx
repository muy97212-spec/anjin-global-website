import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Anjin Global — FDA-Registered Pet Supplement Manufacturer',
  description:
    'cGMP-compliant OEM/ODM pet supplement manufacturing. 16+ years of formulation expertise, FDA registered, GMP certified.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
